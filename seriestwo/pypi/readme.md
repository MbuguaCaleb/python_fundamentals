**Pypi-Python Package INDEX**

```
These are packages that have been developed and
published by other people for us to use.

We do not have to reinvent a wheel from scratch


Pip is a tool that comes with python and we use it
as a package manager to store packages from python

Once we install a package via PIP we may import it Just
like we do for the normal packages,



```
